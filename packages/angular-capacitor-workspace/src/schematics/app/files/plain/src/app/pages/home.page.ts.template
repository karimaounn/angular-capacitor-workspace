import { ChangeDetectionStrategy, Component } from '@angular/core';

/**
 * The starter screen. It is here to be deleted — replace it with your own home
 * route in `app.routes.ts`.
 */
@Component({
  selector: '<%= prefix %>-home-page',
  changeDetection: ChangeDetectionStrategy.OnPush,
  styles: `
    :host {
      display: block;
      max-inline-size: 60rem;
      margin-inline: auto;
    }

    ul {
      padding-inline-start: 1.25rem;
    }

    li + li {
      margin-block-start: 0.5rem;
    }

    code {
      font-family: ui-monospace, monospace;
      font-size: 0.9em;
    }
  `,
  template: `
    <h1><%= title %></h1>
    <p>Generated by <code>angular-capacitor-workspace</code>.</p>
    <ul>
      <li>Replace this page: <code>src/app/pages/home.page.ts</code>.</li>
      <li>House rules for this workspace: <code>AGENTS.md</code>.</li>
      <li>Every script, with a line on each: <code>README.md</code>.</li>
      <li>
        For design tokens, light and dark themes and a switcher, add the design-system
        library: <code>ng g angular-capacitor-workspace:ui-lib ui</code>.
      </li>
    </ul>
  `,
})
export class HomePage {}
