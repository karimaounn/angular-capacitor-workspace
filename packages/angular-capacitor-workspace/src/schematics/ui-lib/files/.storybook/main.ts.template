import type { StorybookConfig } from '@storybook/angular';

const config: StorybookConfig = {
  stories: ['../src/**/*.stories.@(ts|mdx)'],

  addons: ['@storybook/addon-docs'],

  // Holds tokens.css, compiled by `npm run styles:tokens` and linked from
  // preview-head.html. Generated, and gitignored.
  staticDirs: ['./static'],

  framework: {
    name: '@storybook/angular',
    options: {},
  },

  docs: {
    // `tags: ['autodocs']` on a meta opts that component in. Opt-in rather than
    // global so a work-in-progress story does not publish a docs page.
    defaultName: 'Docs',
  },
};

export default config;
