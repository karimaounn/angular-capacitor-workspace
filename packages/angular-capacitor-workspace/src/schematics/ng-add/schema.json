{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "acw-ng-add",
  "title": "Add the workspace overlay to an existing workspace",
  "type": "object",
  "properties": {
    "e2e": {
      "oneOf": [
        { "type": "string", "enum": ["playwright"] },
        { "type": "boolean", "enum": [false] }
      ],
      "default": false
    },
    "uiLib": {
      "type": "string",
      "description": "Also generate a design-system library with this name."
    }
  },
  "additionalProperties": false
}
